{
  "name": "artours",
  "version": "1.0.0",
  "description": "AR Tours - Melbourne's Premier Augmented Reality Tour Company",
  "private": true,
  "homepage": "./",
  "dependencies": {
    "framer-motion": "^12.42.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.14.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "node scripts/generate-ai-feeds.js && react-scripts start",
    "build": "node scripts/generate-ai-feeds.js && react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "generate-ai-feeds": "node scripts/generate-ai-feeds.js"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
